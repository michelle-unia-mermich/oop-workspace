#include <time.h>
srand(static_cast<unsigned int>(time(0)));
rand()%(gridWidth)

std::tuple<int,int> pos1;
std::get<0>(pos1)

std::vector<Spot*> grid;
this->grid.push_back(new Persona(xCoord,yCoord));

unsigned counterRemoveShip=0; 
for (auto* entity: this->entities) 
{

    if(some condition is met)
    {
        delete this->entities.at(counterRemoveShip); 
        this->entities.erase(this->entities.begin()+counterRemoveShip); 
        counterRemoveShip--;
    }
       
    counterRemoveShip++;
}

dynamic_cast<Persona*>(spot1)

